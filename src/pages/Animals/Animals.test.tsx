import mockAxios from 'jest-mock-axios';
import { render, screen } from '@testing-library/react';
import Animals from '.';

test('render Animals page', () => {
  // render(<Animals />);
  // const card = screen.getByRole('img', { name: 'animal' });
  // expect(card).toHaveLength(20);
});
